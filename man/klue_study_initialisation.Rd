% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/08_study.R
\name{klue_study_initialisation}
\alias{klue_study_initialisation}
\title{Monte Carlo: initialisation feature ablation}
\usage{
klue_study_initialisation(
  n_random = 50,
  n_cond = 40,
  attr_corr = NULL,
  verbose = TRUE,
  dgp = DGP_DEFAULT,
  blocked = FALSE,
  n_cards = 48L,
  n_blocks = 4L,
  seg_scale = NULL,
  T_random = 20L,
  n_cores = .klue_cores()
)
}
\arguments{
\item{n_random}{number of uninformed random starts per condition.}

\item{n_cond}{number of conditions to run from the H1 grid.}

\item{attr_corr}{optional attribute correlation passed to the simulator;
\code{NULL} for uncorrelated attributes.}

\item{verbose}{if \code{TRUE}, print a summary.}

\item{dgp}{data-generating-process specification.}

\item{blocked}{if \code{TRUE}, use a shared blocked D-efficient design;
otherwise use the per-respondent random-attribute path.}

\item{n_cards}{number of design cards for the blocked design.}

\item{n_blocks}{number of blocks for the blocked design.}

\item{seg_scale}{optional segment-membership scaling passed to the simulator.}

\item{T_random}{number of tasks per respondent in the random-attribute path.}

\item{n_cores}{cores for condition-level parallelism.}
}
\value{
A data.frame with one row per valid condition: true class count K,
  kappa, sigma, whether the RP-contrast and one-hot starts reach the global
  optimum, the per-start fraction of random starts at the global optimum, the
  RP, one-hot, and best log-likelihoods, and the RP-minus-one-hot gap.
}
\description{
Three-arm ablation comparing clustering starts built from RP contrasts, from
one-hot indicators, and uninformed random starts, recording how often each
arm reaches the global log-likelihood (best across all arms).
}
