% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/08_study.R
\name{klue_study}
\alias{klue_study}
\title{Run the full Frings (2026) Monte Carlo study}
\usage{
klue_study(
  run_main = TRUE,
  run_mmnl = TRUE,
  run_supp = TRUE,
  verbose = TRUE,
  dgp = DGP_DEFAULT,
  n_cores = .klue_cores()
)
}
\arguments{
\item{run_main}{if \code{TRUE}, run the main class-count recovery arm.}

\item{run_mmnl}{if \code{TRUE}, run the MNL vs LCMNL vs MMNL comparison.}

\item{run_supp}{if \code{TRUE}, run the supplementary robustness arms.}

\item{verbose}{if \code{TRUE}, print progress and per-arm summaries.}

\item{dgp}{data-generating-process specification forwarded to every driver.}

\item{n_cores}{cores for condition-level parallelism, forwarded to every
driver. \code{n_cores = 1} runs fully sequentially (lowest memory,
reproducible timing, friendly to a shared machine); the default
\code{.klue_cores()} parallelises across conditions. Output is identical
either way (per-condition seeds). Set it to match your time budget.}
}
\value{
A named list of the result data.frames for each arm that was run
  (\code{main}, \code{mmnl}, and the supplementary arms).
}
\description{
Master driver that runs the main arm, the MMNL comparison, and the
supplementary robustness arms, writing each result to a CSV in
\code{OUTPUT_DIR} and returning all results in one list.
}
