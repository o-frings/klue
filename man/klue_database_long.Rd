% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/06_database.R
\name{klue_database_long}
\alias{klue_database_long}
\title{Build the canonical database from long-format data}
\usage{
klue_database_long(
  data,
  id_col,
  task_col,
  alt_col,
  choice_col,
  attribute_cols,
  price_col,
  choice_format = c("indicator", "alt_index"),
  avail_col = NULL,
  scalings = NULL,
  price_scaling = 1,
  verbose = TRUE
)
}
\arguments{
\item{data}{A data.frame, or a single file path to a `.dat`
(whitespace-delimited, header) or `.csv` file to read.}

\item{id_col}{Name of the respondent identifier column.}

\item{task_col}{Name of the choice-task identifier column (unique within
respondent).}

\item{alt_col}{Name of the alternative identifier column within a task.}

\item{choice_col}{Name of the choice column, interpreted per
`choice_format`.}

\item{attribute_cols}{Character vector of generic attribute column names;
each becomes the `x*_*` block in the order supplied.}

\item{price_col}{Length-one name of the price column.}

\item{choice_format}{Either "indicator" (0/1, one chosen row per task) or
"alt_index" (the chosen alternative index repeated on every row of the
task).}

\item{avail_col}{Optional name of an availability column (0/1); tasks where
not all alternatives are available are dropped.}

\item{scalings}{Optional named list of positive divisors keyed by attribute
or price column name; matching columns are divided before use.}

\item{price_scaling}{Positive divisor applied to the price column; shortcut
for adding the price column to `scalings`.}

\item{verbose}{Logical; if `TRUE`, print drop counts and a build summary.}
}
\value{
A data.frame in the canonical wide format with columns `ID`,
  `TASK`, the generic attribute blocks `x1_1..xN_J`, the price columns
  `price_1..price_J`, and `CHOICE` (the chosen alternative index in
  `1..J`). The last alternative is the reference. Attributes `attr_labels`
  (attribute and price labels, suffixed by scaling where applicable),
  `n_alternatives` (J), and `n_generic` (number of generic attributes) are
  attached.
}
\description{
One row per (respondent, task, alternative). `choice_format` is
"indicator" (0/1 on the chosen row) or "alt_index" (chosen alt repeated on
every row of the task). `scalings` is a named list of divisors keyed by
attribute/price column name; `price_scaling` is a shortcut for the price
column. Tasks where any alternative is unavailable (avail_col == 0) and
respondents with fewer tasks than the panel maximum are dropped.
}
