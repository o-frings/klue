% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/08_study.R
\name{klue_study_estimator}
\alias{klue_study_estimator}
\title{Monte Carlo: estimator x start-strategy benchmark}
\usage{
klue_study_estimator(
  n_random = 50L,
  n_perturb = 50L,
  n_cond = 36L,
  jitter_sd = 0.5,
  attr_corr = NULL,
  verbose = TRUE,
  dgp = DGP_DEFAULT,
  blocked = FALSE,
  n_cards = 48L,
  n_blocks = 4L,
  seg_scale = NULL,
  T_random = 20L,
  cond_idx = NULL,
  n_cores = .klue_cores()
)
}
\arguments{
\item{n_random}{number of diffuse random starts per condition.}

\item{n_perturb}{number of perturbation and random-partition starts per
condition.}

\item{n_cond}{number of conditions to run from the H1 grid.}

\item{jitter_sd}{standard deviation of the MNL perturbation jitter.}

\item{attr_corr}{optional attribute correlation passed to the simulator;
\code{NULL} for uncorrelated attributes.}

\item{verbose}{if \code{TRUE}, print a summary.}

\item{dgp}{data-generating-process specification.}

\item{blocked}{if \code{TRUE}, use a shared blocked D-efficient design;
otherwise use the per-respondent random-attribute path.}

\item{n_cards}{number of design cards for the blocked design.}

\item{n_blocks}{number of blocks for the blocked design.}

\item{seg_scale}{optional segment-membership scaling passed to the simulator.}

\item{T_random}{number of tasks per respondent in the random-attribute path.}

\item{cond_idx}{optional vector of condition indices for chunked or
checkpointed runs; \code{NULL} uses the first \code{n_cond} conditions.}

\item{n_cores}{cores for condition-level parallelism.}
}
\value{
A data.frame with one row per estimator x strategy x condition cell:
  true class count K, kappa, sigma, rep, estimator, strategy, number of
  starts, per-start at-global rate, whether the global optimum was reached,
  starts-to-global, and the best and global log-likelihoods.
}
\description{
Crosses two estimators (maximum likelihood and EM) with four start
strategies (clustering, MNL perturbation, random partition, diffuse random),
recording per-start at-global rate, best-of-budget success, and the number of
starts needed to reach the global optimum (the best log-likelihood across all
cells of a condition).
}
