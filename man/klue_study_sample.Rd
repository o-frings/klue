% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/08_study.R
\name{klue_study_sample}
\alias{klue_study_sample}
\title{Monte Carlo: sensitivity to sample size and panel length}
\usage{
klue_study_sample(
  verbose = TRUE,
  dgp = DGP_DEFAULT,
  blocked = FALSE,
  n_cores = .klue_cores()
)
}
\arguments{
\item{verbose}{if \code{TRUE}, print accuracy for each sample-size cell.}

\item{dgp}{data-generating-process specification.}

\item{blocked}{if \code{TRUE}, use a per-panel-length blocked D-efficient
design; otherwise use the per-respondent random-attribute path.}

\item{n_cores}{cores for condition-level parallelism.}
}
\value{
A data.frame with one row per cell: tasks per respondent (T_tasks),
  respondents per class (N_per_class), and BIC class-count accuracy (percent).
}
\description{
Tests BIC class-count recovery across a grid of respondents per class and
tasks per respondent. Under blocking, the number of blocks is held fixed and
cards per block vary with panel length so panel length is not confounded with
design diversity.
}
