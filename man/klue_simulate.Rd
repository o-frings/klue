% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/01_simulate.R
\name{klue_simulate}
\alias{klue_simulate}
\alias{klue_simulate_cov}
\title{Simulate panel choice data from the Frings (2026) DGP}
\usage{
klue_simulate(
  N_per_class = 150,
  T_tasks = 20,
  true_K = 2,
  separation = 1,
  heterogeneity = 0.25,
  seed = 12345,
  class_proportions = NULL,
  dgp = DGP_DEFAULT,
  sep_profile = NULL,
  attr_corr = NULL,
  design = NULL,
  seg_scale = NULL,
  covariates = FALSE,
  covariate_strength = 1
)

klue_simulate_cov(
  N_per_class = 150,
  T_tasks = 20,
  true_K = 2,
  separation = 1,
  heterogeneity = 0.25,
  seed = 12345,
  covariate_strength = 1,
  dgp = DGP_DEFAULT,
  design = NULL
)
}
\arguments{
\item{N_per_class}{Number of respondents per latent class.}

\item{T_tasks}{Number of choice tasks per respondent. Overridden by the
block size when \code{design} is supplied.}

\item{true_K}{Number of latent classes.}

\item{separation}{Scalar controlling the distance of class means from the
grand mean \code{beta_bar}.}

\item{heterogeneity}{Within-class standard deviation of the individual
coefficients.}

\item{seed}{Integer seed for reproducible simulation.}

\item{class_proportions}{Optional numeric vector of class shares summing to
one. When \code{NULL}, classes are equally sized. Ignored when
\code{covariates = TRUE}.}

\item{dgp}{Data-generating-process specification supplying \code{n_beta},
\code{beta_bar}, \code{n_alternatives}, and \code{n_generic}.}

\item{sep_profile}{Optional numeric vector of per-attribute separation
weights. When \code{NULL}, all attributes are weighted equally. Ignored
when \code{covariates = TRUE}.}

\item{attr_corr}{Optional attribute correlation: \code{NULL} (independent),
a scalar equicorrelation, or a full correlation matrix. Applies only to
the random (non-design) path and is ignored when \code{covariates = TRUE}.}

\item{design}{Optional blocked design from \code{klue_design()}. When
supplied, respondents share their block's cards.}

\item{seg_scale}{Optional per-class scale (recycled to length
\code{true_K}) placing segments at unequal distances from the grand mean.
Ignored when \code{covariates = TRUE}.}

\item{covariates}{Logical; when \code{TRUE}, class membership is driven by
covariates Z1 ~ N(0, 1) and a binary Z2 rather than fixed proportions.}

\item{covariate_strength}{Scalar scaling the covariate effect on class
membership when \code{covariates = TRUE}.}
}
\value{
A list with elements \code{database} (the simulated panel with a
  \code{CHOICE} column), \code{true_betas}, \code{true_class},
  \code{individual_betas}, \code{N}, \code{T}, \code{K}, and \code{dgp};
  when \code{covariates = TRUE} it also contains \code{Z1} and \code{Z2}
  (and adds these columns to \code{database}).
}
\description{
One entry point for the plain DGP and the concomitant-covariate DGP
(\code{covariates = TRUE}; class membership driven by Z1 ~ N(0,1) and a
binary Z2). With \code{design} from \code{klue_design()}, respondents share
their block's cards; otherwise attributes are drawn fresh per row,
optionally correlated via \code{attr_corr}.
}
