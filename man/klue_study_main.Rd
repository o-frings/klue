% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/08_study.R
\name{klue_study_main}
\alias{klue_study_main}
\title{Main Monte Carlo: class-count recovery across the condition grid}
\usage{
klue_study_main(
  true_K_values = c(1, 2, 3, 4, 5),
  kappa_values = c(0.5, 0.75, 1, 1.25, 1.5),
  sigma_values = c(0.1, 0.15, 0.2, 0.25),
  n_reps = 5,
  C_cands = 1:6,
  dgp = DGP_DEFAULT,
  sep_profile = NULL,
  blocked = TRUE,
  n_cards = 48L,
  n_blocks = 4L,
  seg_scale = NULL,
  T_random = 20L,
  verbose = TRUE,
  n_cores = .klue_cores()
)
}
\arguments{
\item{true_K_values}{true class counts to simulate.}

\item{kappa_values}{segment-separation values (kappa).}

\item{sigma_values}{within-class heterogeneity values (sigma).}

\item{n_reps}{replications per condition.}

\item{C_cands}{candidate class counts fitted in each condition.}

\item{dgp}{data-generating-process specification.}

\item{sep_profile}{optional separation profile passed to the simulator.}

\item{blocked}{if \code{TRUE}, use one shared blocked D-efficient design with
T per respondent equal to the block size; if \code{FALSE}, use the
per-respondent random-attribute path with T equal to \code{T_random}.}

\item{n_cards}{number of design cards for the blocked design.}

\item{n_blocks}{number of blocks for the blocked design.}

\item{seg_scale}{optional segment-membership scaling passed to the simulator.}

\item{T_random}{number of tasks per respondent in the random-attribute path.}

\item{verbose}{if \code{TRUE}, print per-condition progress.}

\item{n_cores}{cores for condition-level parallelism.}
}
\value{
A data.frame with one row per condition: condition index, true_K,
  kappa, sigma, rep, the BIC/AIC/ICL selected class counts and correctness
  flags, the best clustering method, and ARI, RMSE, bias, and ICL_BIC values.
}
\description{
Runs the primary arm: simulate data over a grid of true class counts,
segment separation, and within-class heterogeneity, then estimate LCMNL for
each candidate class count and record whether BIC, AIC, and ICL select the
true class count, plus parameter and clustering recovery.
}
