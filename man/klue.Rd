% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/07_workflow.R
\name{klue}
\alias{klue}
\title{Run the full LCMNL specification workflow}
\usage{
klue(
  database = NULL,
  data = NULL,
  format = c("auto", "long", "wide"),
  C_cands = 1:6,
  run_mmnl = TRUE,
  run_mmnl_corr = FALSE,
  mmnl_opts = list(),
  attr_labels = NULL,
  output_prefix = "workflow",
  output_dir = NULL,
  write_csv = TRUE,
  verbose = TRUE,
  n_cores = 1L,
  ...
)
}
\arguments{
\item{database}{canonical wide-format data frame with columns ID, TASK,
CHOICE plus attribute columns. Supply this or `data`, not both.}

\item{data}{raw data frame passed to \code{klue_database} together with the
column-mapping arguments in `...` when `database` is not supplied.}

\item{format}{one of "auto", "long", "wide": the layout of `data`, forwarded
to \code{klue_database}. Default "auto".}

\item{C_cands}{integer vector of class counts to estimate. Default 1:6.}

\item{run_mmnl}{logical; if TRUE estimate the independent-normals MMNL
benchmark. Default TRUE.}

\item{run_mmnl_corr}{logical; if TRUE estimate the correlated-normals MMNL
benchmark. Default FALSE.}

\item{mmnl_opts}{named list of extra arguments forwarded to \code{klue_mmnl}
for both MMNL flavours. Default an empty list.}

\item{attr_labels}{character vector of length `n_generic + 1` naming the
attribute coefficients (the last entry is price). If NULL, taken from the
database attribute or generated as x1, x2, ..., price.}

\item{output_prefix}{character prefix for the written CSV file names.
Default "workflow".}

\item{output_dir}{directory for the written CSVs. If NULL, uses OUTPUT_DIR.}

\item{write_csv}{logical; if TRUE write the summary, class-coefficient and
model-comparison CSVs. Default TRUE.}

\item{verbose}{logical; if TRUE print progress and result tables. Default
TRUE.}

\item{n_cores}{cores for the per-start LCMNL fits within each C (forwarded
to \code{klue_lcmnl}). Default 1 (sequential). Setting it to the number of
physical cores fits the six clustering starts concurrently and is the main
speed lever for a single dataset (~3x on a typical multicore machine).}

\item{...}{column-mapping and scaling arguments forwarded to
\code{klue_database} when `data` is supplied instead of `database`.}
}
\value{
Invisibly, a list with elements `database`, `dgp`, `lcmnl` (the
  per-C fits), `mmnl`, `mmnl_corr`, `summary`, `class_betas`, `comparison`,
  `best_C` (the BIC-best class count) and `best_lcmnl` (the BIC-best fit).
}
\description{
Three calling conventions: (1) `database =` canonical wide format;
(2) `data =` + long-format column mapping; (3) `data =` + wide-format
column mapping (both forwarded to \code{klue_database}). Loops over
`C_cands`, optionally estimates the MMNL benchmarks, prints and writes
summary CSVs, and returns the full results list invisibly.
}
