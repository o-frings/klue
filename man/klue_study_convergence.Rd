% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/08_study.R
\name{klue_study_convergence}
\alias{klue_study_convergence}
\title{Monte Carlo: clustering start vs random restarts (convergence)}
\usage{
klue_study_convergence(
  n_random = 50,
  n_cond = 40,
  verbose = TRUE,
  dgp = DGP_DEFAULT,
  blocked = FALSE,
  n_cards = 48L,
  n_blocks = 4L,
  seg_scale = NULL,
  T_random = 20L,
  n_cores = .klue_cores()
)
}
\arguments{
\item{n_random}{number of diffuse random restarts per condition.}

\item{n_cond}{number of conditions to run from the H1 grid.}

\item{verbose}{if \code{TRUE}, print a summary.}

\item{dgp}{data-generating-process specification.}

\item{blocked}{if \code{TRUE}, use a shared blocked D-efficient design;
otherwise use the per-respondent random-attribute path.}

\item{n_cards}{number of design cards for the blocked design.}

\item{n_blocks}{number of blocks for the blocked design.}

\item{seg_scale}{optional segment-membership scaling passed to the simulator.}

\item{T_random}{number of tasks per respondent in the random-attribute path.}

\item{n_cores}{cores for condition-level parallelism.}
}
\value{
A data.frame with one row per converged condition: true class count
  K, whether the clustering start is best, the fraction of random starts at
  the global and at local optima, the first random start that matches the
  clustering log-likelihood, and the clustering-to-random time ratio.
}
\description{
Tests whether the clustering-based start reaches the same log-likelihood
optimum as a budget of diffuse random restarts, and how many random starts
are needed to match it.
}
