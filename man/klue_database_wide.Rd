% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/06_database.R
\name{klue_database_wide}
\alias{klue_database_wide}
\title{Build the canonical database from wide-format data}
\usage{
klue_database_wide(
  data,
  id_col,
  task_col = NULL,
  choice_col,
  attribute_cols = NULL,
  price_col = NULL,
  avail_col = NULL,
  scalings = NULL,
  attributes = NULL,
  price = NULL,
  availability = NULL,
  verbose = TRUE
)
}
\arguments{
\item{data}{A data.frame, or a single file path to a `.dat`
(whitespace-delimited, header) or `.csv` file to read.}

\item{id_col}{Name of the respondent identifier column.}

\item{task_col}{Optional name of the choice-task identifier column; if
`NULL`, tasks are numbered sequentially within each respondent.}

\item{choice_col}{Name of the column holding the chosen alternative index
in `1..J`.}

\item{attribute_cols}{Named list of length-J character vectors of column
names, one vector per generic attribute; `NA` marks a structural zero for
that alternative. The list element names become attribute labels.}

\item{price_col}{Length-J character vector of per-alternative price column
names.}

\item{avail_col}{Optional length-J character vector of per-alternative
availability column names (0/1); tasks where not all alternatives are
available are dropped.}

\item{scalings}{Optional named list of positive divisors keyed by attribute
name or "price"; matching columns are divided before use.}

\item{attributes}{Deprecated alias for `attribute_cols`.}

\item{price}{Deprecated alias for `price_col`.}

\item{availability}{Deprecated alias for `avail_col`.}

\item{verbose}{Logical; if `TRUE`, print drop counts and a build summary.}
}
\value{
A data.frame in the canonical wide format with columns `ID`,
  `TASK`, the generic attribute blocks `x1_1..xN_J`, the price columns
  `price_1..price_J`, and `CHOICE` (the chosen alternative index in
  `1..J`). The last alternative is the reference. Attributes `attr_labels`
  (attribute and price labels, suffixed by scaling where applicable),
  `n_alternatives` (J), and `n_generic` (number of generic attributes) are
  attached.
}
\description{
One row per (respondent, task). `attribute_cols` is a named list; each
entry is a length-J character vector of column names (NA = structural zero
on that alternative). `price_col` is a length-J vector; `avail_col`
optionally too. `scalings` is keyed by attribute name or "price".
}
